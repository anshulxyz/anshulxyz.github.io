- What is sorting
- Selection sort vs Bubble Sort
- Why is Selection sort faster despite have the same runtime O(n^2)
- Calculate the average and best case scenarios
- Different types of selection sort
- what algorithm is used by Scala's sort method on Lists
