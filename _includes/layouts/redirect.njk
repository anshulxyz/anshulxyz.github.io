<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="refresh" content="0; url={{ redirect }}">
    <title>Redirecting…</title>
    <link rel="canonical" href="{{ redirect }}">
  </head>
  <body>
    <h1>Redirecting…</h1>
    <p>
      This page has moved to <a href="{{ redirect }}">{{ redirect }}</a>.
    </p>
  </body>
</html>
